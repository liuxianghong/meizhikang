//
//  RectView.swift
//  meizhikang
//
//  Created by 刘向宏 on 16/1/9.
//  Copyright © 2016年 刘向宏. All rights reserved.
//

import UIKit

class RectView: UIView {

    //var color : UIColor = UIColor.redColor()
    // Only override drawRect: if you perform custom drawing.
    // An empty implementation adversely affects performance during animation.
//    override func drawRect(rect: CGRect) {
        // Drawing code
//        let context = UIGraphicsGetCurrentContext()
//        let width = rect.size.width;
//        let height = rect.size.height;
//        CGContextSetFillColorWithColor(context, color.CGColor)
//        CGContextAddArc(context, width/2, width/2, width/2, CGFloat(M_PI), 2*CGFloat(M_PI), 0); //添加一个圆
//        CGContextDrawPath(context, .Fill);//绘制填充
//        CGContextAddArc(context, width/2, height - width/2, width/2, 0, CGFloat(M_PI), 0); //添加一个圆
//        CGContextDrawPath(context, .Fill);//绘制填充
//        CGContextAddRect(context, CGRectMake(0, width/2, width, height - width));
//        CGContextDrawPath(context, .Fill);//绘制填充
//    }


}
